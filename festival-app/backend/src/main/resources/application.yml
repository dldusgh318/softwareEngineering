server:
  port: 8080

spring:
  application:
    name: festival-backend

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://127.0.0.1:3000
  jwt:
    secret: ${JWT_SECRET:local-development-jwt-secret-key-32bytes}
    expiration-millis: ${JWT_EXPIRATION_MILLIS:3600000}
